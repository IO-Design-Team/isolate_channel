## How to run tests

1. Clone [this branch](https://github.com/Rexios80/isolate_name_server/tree/feature/macos)
2. Run `build.sh`
3. Copy `native/libnameserver.dylib` from that project into the same location in this one
